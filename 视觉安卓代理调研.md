# Python 通用视觉 LLM 操控 Android 调研与原型说明

更新时间：2026-08-27（Asia/Shanghai）

## 结论

当前最适合这个 Android 点击测试场景的工程链路是，Waydroid 只是其中一种设备运行时：

```text
ADB 截图 + UIAutomator 层级
        -> 视觉模型只输出一个严格 JSON 动作
        -> Python 校验动作和坐标
        -> ADB input 执行
        -> 下一轮截图/层级确认
```

原因是 ADB 在 Android 真机、模拟器和 Waydroid 上都容易复用，截图和点击接口稳定；UIAutomator 层级可以提供文本、描述、资源 ID 和 bounds 作为视觉模型的辅助；视觉模型负责跨应用的语义判断和截图定位，而不是直接执行任意命令。后续可把 ADB actor 换成 `uiautomator2` 的元素点击，以减少坐标误差；Appium 更适合已有跨平台测试基础设施的团队，但服务端和驱动的部署复杂度更高。

## 候选方案比较

| 方案 | 优点 | 局限 | 建议 |
| --- | --- | --- | --- |
| ADB `screencap` + `input` | 依赖少、跨设备、适合 Waydroid、易审计 | 需要自己做坐标校验和状态断言 | 当前原型的执行层 |
| `uiautomator2` | Python API、可读无障碍树、元素 bounds 和点击 | 需要部署设备端服务，部分 Compose/自绘 UI 仍需视觉 | 下一步增强定位和断言 |
| Appium + UiAutomator2 driver | 跨平台、动作 API 和 CI 集成成熟 | 服务端/驱动较重，单动作延迟可能更高 | 已有 Appium 基础设施时采用 |
| AndroidWorld | 提供 ADB 观测/动作和成功断言的研究范式 | 主要是评测环境，不是直接的生产控制库 | 借鉴任务状态验证设计 |
| OmniParser/GUI grounding | 适合纯视觉元素定位 | 模型、分辨率、裁剪和中文小字会影响精度 | 作为本地视觉解析增强，不单独承担执行 |

### 2026-08 实时核对

- `openatx/uiautomator2` 官方仓库的最新发布为 3.7.0（2026-06-26）；3.x 继续提供 Python 封装，并新增/增强 `u2cli` 和层级转储能力。对本项目而言，后续可以把当前 ADB XML 读取替换为 `uiautomator2` 元素定位，但仍需保留截图作为 Compose、自绘 WebView 和无障碍树缺失时的后备观测。
- Appium 官方 3.1 文档仍将 UiAutomator2 作为 Android 驱动，并提供 Python 客户端路径和 `appium driver doctor uiautomator2` 预检。它适合已有多端测试基础设施的团队；本机单 Waydroid 实例直接使用 ADB 的部署成本更低。
- Google Research 的 AndroidWorld 官方仓库当前描述为 116 个任务、20 个真实应用、动态参数化和持久化奖励信号，并要求代理在每轮读取屏幕/UI 状态后返回带 `done` 的交互结果。本原型采用同样的“观测 -> 单动作 -> 再观测 -> 本地断言”节奏，但不把 AndroidWorld 当作生产控制库。
- Microsoft OmniParser 官方仓库在 2025-02 发布 V2；检索到 2026-07 的更新加入 YOLOv9-E 交互区域检测权重。它适合增强纯视觉元素 grounding，但额外模型推理和中文小字误检仍使其不宜替代当前的 UIAutomator + ADB 执行层。

## 本地 OpenAI 兼容端点

本机 `http://localhost:7860/v1/models` 当前配置使用 `gemini-3.5-flash-lite`。Python `openai` 客户端使用 `base_url` 连接自定义端点，并通过 Chat Completions 的 `image_url` data URL 传递 PNG 截图。原型默认模型是 `gemini-3.5-flash-lite`，也可用 `LOCAL_LLM_MODEL` 覆盖；`REASONING_EFFORT` 通过 Chat Completions 顶层字段透传，默认使用 `high`。该端点属于运行者自己的本地服务，项目不提供模型服务。

密钥只从环境变量 `LOCAL_LLM_API_KEY` 读取，不写入代码、配置、日志或本文档。

## 同轮上下文与前缀缓存

`元宝每日任务.py` 和 `视觉安卓代理.py` 都在一次进程运行期间保留回合历史。历史只包含页面阶段摘要、规范化动作和执行结果；完整 UI、旧截图、账号输入和验证码不会回放。每次请求把当前编排状态、无障碍层级和唯一最新截图放在最后，静态 system 提示保持不变。

这样既让模型知道本轮已经尝试过什么，也避免视觉输入随步数线性复制。请求不携带 Claude 专用缓存字段或供应商私有 cache key；缓存复用依赖稳定的模型、提示、工具定义和消息前缀。抓包与 `tiktoken` 估算见[请求前缀缓存与上下文](docs/请求前缀缓存与上下文.md)。

## 原型的安全边界

- ADB 目标必须显式指定。主任务脚本的 `ANDROID_DEVICE=auto` 仅用于自动发现 Waydroid；也可以填写 `emulator-5554`、USB 序列号或 `<主机>:5555`，不会使用其他离线设备。
- 模型只能返回 `tap`、`swipe`、`type`、`key`、`wait`、`done`。
- 点击/滑动坐标必须在当前 `750x1333` 屏幕范围内。
- `key` 仅允许回车、返回、Tab 和删除。
- 每一步执行前重新截图并读取 UI 层级；执行后重新进入下一轮观测。
- 有步骤上限，避免模型在页面异常时无限操作。
- 同一页面状态连续重复同一非等待动作达到阈值时自动熔断，避免网页活动返回后循环点击。
- 对“输入 1 并发送”目标要求两个本地证据：聊天区出现用户消息 `1`，且输入框恢复“发消息或按住说话...”占位符；模型单独返回 `done` 不足以结束。
- 不读取或记录手机号、验证码、二维码、Cookie、Token。

## 元宝福利中心的实际跳转

实测版本 `2.79.10` 中，福利中心顶部“每日问元宝得积分”的“去提问”按钮，以及下方“问元宝问题”的“去提问”按钮，点击后都会关闭 `WebBrowserActivity` 并返回“我们”页，不会自动切换底部导航。要继续提问，需在返回后点击底部“问元宝”进入会话页，再定位输入框和“发送消息”按钮。该行为已写入视觉代理的系统提示，避免把返回页面误判为没有进展。

## 参考来源

- ADB：Android Debug Bridge 官方文档，https://developer.android.com/tools/adb
- uiautomator2：项目仓库，https://github.com/openatx/uiautomator2
- Appium：官方文档，https://appium.io/docs/en/
- AndroidWorld：Google Research 仓库，https://github.com/google-research/android_world
- Vision-driven Mobile GUI Testing：https://arxiv.org/html/2407.03037v1
- 手机 GUI Agent 资料汇总，https://github.com/PhoneLLM/Awesome-LLM-Powered-Phone-GUI-Agents
- OpenAI Python 自定义 `base_url`、视觉图片输入，https://github.com/openai/openai-python
- uiautomator2 3.7.0 发布记录，https://github.com/openatx/uiautomator2/releases
- Appium 3.1 UiAutomator2 驱动文档，https://appium.io/docs/en/3.1/quickstart/uiauto2-driver
- AndroidWorld 官方仓库，https://github.com/google-research/android_world
- OmniParser 官方仓库，https://github.com/microsoft/OmniParser

## 运行

使用本机 miniconda Python，不把密钥放在命令历史中：

```bash
read -r -s LOCAL_LLM_API_KEY
export LOCAL_LLM_API_KEY
export LOCAL_LLM_BASE_URL=http://localhost:7860/v1
export LOCAL_LLM_MODEL=gemini-3.5-flash-lite
export REASONING_EFFORT=high
.venv/bin/python 视觉安卓代理.py --device "$ANDROID_DEVICE"
unset LOCAL_LLM_API_KEY
```

运行早期原型前需保证 `ANDROID_DEVICE` 对应设备已经启动，并且 `adb -s <DEVICE> get-state` 返回 `device`。例如：

```bash
export ANDROID_DEVICE=<主机>:5555
adb connect "$ANDROID_DEVICE"
adb -s "$ANDROID_DEVICE" get-state
.venv/bin/python 视觉安卓代理.py --device "$ANDROID_DEVICE"
```

完成“福利中心 -> 问元宝 -> 输入 1 -> 发送”后，模型应返回 `done`，程序随即停止。需要自动启动/停止 Waydroid 时，请使用 `元宝每日任务.py` 主脚本和 `ANDROID_DEVICE=auto`。
