[Unit]
StartLimitIntervalSec=2h
StartLimitBurst=5

[Service]
# Type=oneshot 允许 on-failure；仅在本轮失败时重试，成功后不会常驻重启。
Restart=on-failure
RestartSec=90s
# 登录或协议确认属于人工前置条件；达到内部重试上限的退出码 76
# 也不重复拉起同一页面，失败窗口由人工关闭。
RestartPreventExitStatus=75 76
